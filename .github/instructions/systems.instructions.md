---
applyTo: "src/morningstar_modbus/systems/**/*.py,src/morningstar_modbus/api/routers/systems.py,docs/component-graph.md,docs/system-api.md"
---

Load `.agents/skills/system-topology-and-power/SKILL.md` and `testing-and-ci`.

Preserve immutable `controller_uid`, metric-specific aggregation, complete/partial/empty quality, source
provenance, and the distinction between transport topology and evidence-backed component relationships.
Reconcile ReadyEdge products by strong identity evidence before adding reported-only components.

Power/energy values must remain observed, derived, or unknown. Do not treat charger current as battery net
current and do not invent load/generator/discharge/residual values to close a balance.
